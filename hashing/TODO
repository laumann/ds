TODO
----
 * implementer auto-fetch af tilfældige tal
 * testing
   - Implementer hash-driver (hotbox.c)
   - From C++ STL we use unordered_map
   - The keys hash are arbitrary length text strings
   - An alternative approach could be to hash 64-bit keys obtained by combining
     8 input characters.

IN PROGRESS
-----------
 * tage tid på vores hashfunktion

DONE
----
 * 8 chars ad gange
